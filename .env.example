# Vite 开发服务器配置
#
# 复制此文件为 .env，按需修改：
#   cp .env.example .env
#
# VITE_PORT  - 开发/预览服务器端口（默认 5173）
# VITE_HOST  - 监听地址（true = 0.0.0.0 允许远程访问，false = 仅 localhost）
#
# EdgeOne 部署时同样可在控制台配置这些变量（构建时注入）

VITE_PORT=5173
VITE_HOST=true
